export type PaintConfiguration = {
  strokeWidth: number;
  fillColor: string;
  strokeColor: string;
  tension: number;
  closed: boolean;
};

export type CanvasToolConfiguration = {};

export type StateToolConfiguration = {};
